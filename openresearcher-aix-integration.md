# OpenResearcher 与 AIX 融合分析

> 项目：https://github.com/TIGER-AI-Lab/OpenResearcher（664 Star）
> 特点：完全开源、低成本、离线深度研究
> 生成日期：2026-04-20

---

## OpenResearcher 解决什么问题？

### 行业痛点

**深度研究智能体的训练瓶颈**：

| 痛点 | 后果 |
|------|------|
| 依赖闭源搜索引擎API | 成本极高 |
| 网络内容实时变动 | 结果难以复现 |
| 缺乏开源训练数据 | 开源社区受阻 |

### OpenResearcher 的解决方案

**完全开源、低成本、可复现的深度研究流水线**：

```
离线语料库（1500万篇文档）
       ↓
自建检索引擎（无API依赖）
       ↓
三个操作原语（Search/Open/Find）
       ↓
教师模型合成轨迹（9.7万条）
       ↓
学生模型微调（30B-A3B）
       ↓
深度研究智能体
```

---

## 核心创新点

### 1. 离线搜索引擎环境

**1500万篇文档，11B-token 语料库**

| 维度 | 传统方案 | OpenResearcher |
|------|----------|----------------|
| 数据源 | Google/Bing API | 自建离线语料库 |
| 成本 | 按调用付费 | 一次部署，零成本 |
| 稳定性 | 网络波动影响 | 完全可控 |
| 复现性 | 难以复现 | 100%可复现 |

### 2. 三个操作原语

**像人类学者一样浏览**：

| 原语 | 含义 | 人类对应行为 |
|------|------|-------------|
| **Search** | 搜索关键词 | 在搜索框输入 |
| **Open** | 打开网页 | 点击链接 |
| **Find** | 查找证据 | 阅读并定位关键信息 |

**示例**：

```
用户问题："量子计算在金融领域的最新应用有哪些？"

OpenResearcher执行：
1. Search("量子计算 金融应用 2024")
2. Open(搜索结果第1篇论文)
3. Find("应用案例")
4. Open(搜索结果第2篇新闻)
5. Find("实际部署")
6. ...（多轮搜索、打开、查找）
7. 综合所有证据，生成答案
```

### 3. 高质量训练数据

**9.7万条深度研究轨迹，每条平均100+轮对话**

| 数据特点 | 说明 |
|----------|------|
| 轨迹数量 | 9.7万条 |
| 平均轮数 | 100+轮 |
| 教师模型 | GPT-OSS-120B（原生浏览器工具） |
| 任务类型 | 复杂多步推理 |

### 4. 性能表现

**30B-A3B 模型在深度研究基准上的表现**：

| 模型 | BrowseComp-Plus准确率 |
|------|----------------------|
| GPT-4.1 | ~45% |
| Claude-Opus-4 | ~48% |
| Gemini-2.5-Pro | ~50% |
| DeepSeek-R1 | ~52% |
| **OpenResearcher-30B** | **54.8%** |

**关键**：30B开源模型超越所有顶尖闭源模型

---

## 与 AIX 的契合点分析

### 契合1：离线搜索引擎 → AIX Box 本地检索

**OpenResearcher**：
- 1500万篇文档，离线检索
- 无需外部API，零成本
- 完全可控，可复现

**AIX Box**：
- 本地IPFS存储
- 数据不出Box
- 离线推理能力

**融合方案**：

```
AIX Box 本地搜索引擎架构：
├── 语料库：那耶村知识库 + 农产品数据 + 创作者语料
├── 检索引擎：OpenResearcher retriever（移植）
├── 操作原语：Search/Open/Find Skill
└── 推理模型：Pico Claw（本地LLM）
```

**那耶村场景**：

```python
用户问题："梯田米最佳种植时间是什么？"

AIX Box本地执行：
1. Search("梯田米 种植时间") → 本地语料库检索
2. Open(那耶村农业知识文档)
3. Find("最佳种植月份")
4. Open(农业局技术指南)
5. Find("气候条件要求")
6. 综合证据，生成答案
```

**Coin Hour消耗**：
- 离线检索：0 CH
- 本地推理：10 CH
- **总计**：10 CH（传统云端API需要50+ CH）

### 契合2：Search/Open/Find → AIX Skill 原语

**OpenResearcher 的三个原语可以Skill化**：

```yaml
---
name: search-open-find-skill
description: 深度研究操作原语，Search/Open/Find三步法
---

# Search/Open/Find Skill

## 操作原语

### Search(query)
- 在本地语料库中搜索
- 返回相关文档列表
- Coin Hour: 0（离线）

### Open(doc_id)
- 打开指定文档
- 加载全文内容
- Coin Hour: 0（离线）

### Find(keywords)
- 在文档中定位关键信息
- 返回相关段落
- Coin Hour: 0（离线）

## 组合使用

1. Search → 获取候选文档
2. Open → 深度阅读
3. Find → 精准定位
4. 多轮迭代 → 综合答案
```

### 契合3：深度研究轨迹 → 那耶村知识库训练

**OpenResearcher的数据合成方法可以复用**：

```
那耶村深度研究轨迹合成：
├── 语料库：村内文档、农产品数据、创作者语料
├── 教师模型：云端大模型（一次性）
├── 任务生成：那耶村常见问题
├── 轨迹合成：Search/Open/Find多轮推理
└── 学生模型：蒸馏到Pico Claw（本地运行）
```

**示例任务**：

```
问题："今年梯田米市场价格趋势如何？"

深度研究轨迹：
1. Search("梯田米 2024 市场价格")
2. Open(农产品批发市场价格报告)
3. Find("稻米价格走势")
4. Search("气候对稻米产量影响")
5. Open(农业气象报告)
6. Find("降雨量 温度")
7. Search("消费需求变化")
8. Open(电商平台销售数据)
9. Find("购买量 同比增长")
10. 综合分析，生成趋势预测
```

### 契合4：模型蒸馏 → AIX Box 本地模型优化

**OpenResearcher 的蒸馏方法**：

| 维度 | 教师模型 | 学生模型 |
|------|----------|----------|
| 参数量 | 120B | 30B |
| 性能 | 顶级 | 超越教师 |
| 成本 | 高 | 低 |

**AIX Box 可以进一步蒸馏**：

| 维度 | 教师模型 | 学生模型 | AIX Box模型 |
|------|----------|----------|-------------|
| 参数量 | 120B | 30B | 7B-13B |
| 性能 | 顶级 | 54.8% | 目标40%+ |
| 成本 | 高 | 中 | **极低** |
| 部署 | 云端 | 服务器 | **本地Box** |

---

## 具体融合方案

### 方案一：AIX Box 本地搜索引擎

**架构**：

```
┌─────────────────────────────────────┐
│        AIX Box 本地搜索引擎          │
├─────────────────────────────────────┤
│                                     │
│  ┌─────────────────────────────┐   │
│  │   语料库（IPFS存储）          │   │
│  │   • 那耶村知识库              │   │
│  │   • 农产品数据                │   │
│  │   • 创作者语料                │   │
│  │   • 行业报告                  │   │
│  └─────────────────────────────┘   │
│                ↓                    │
│  ┌─────────────────────────────┐   │
│  │   检索引擎（移植OpenResearcher）│   │
│  └─────────────────────────────┘   │
│                ↓                    │
│  ┌─────────────────────────────┐   │
│  │   Search/Open/Find Skill     │   │
│  └─────────────────────────────┘   │
│                ↓                    │
│  ┌─────────────────────────────┐   │
│  │   Pico Claw 本地推理          │   │
│  │   （消耗Coin Hour）           │   │
│  └─────────────────────────────┘   │
│                                     │
└─────────────────────────────────────┘
```

**成本对比**：

| 维度 | 云端API方案 | AIX Box本地方案 |
|------|------------|----------------|
| 搜索API调用 | 10次 × $0.01 = $0.10 | 0（离线） |
| LLM推理 | $0.50 | 10 Coin Hour ≈ ¥10 |
| 总成本 | $0.60 ≈ ¥4.3 | ¥10（但囤AIX可产出） |

### 方案二：那耶村深度研究训练

**数据合成流水线**：

```
第一步：构建语料库
├── 收集那耶村文档（农业知识、村规民约、历史资料）
├── 收集农产品数据（价格、产量、气候）
├── 收集创作者语料（艺术家作品、音乐人Demo）
└── 上传到AIX Box IPFS存储

第二步：生成任务
├── 那耶村常见问题（农民、游客、创作者）
├── 农产品相关问题
├── 文化创作相关问题
└── 预测市场相关问题

第三步：轨迹合成（云端教师模型）
├── 使用OpenResearcher方法
├── Search/Open/Find多轮推理
├── 记录完整轨迹
└── 生成高质量训练数据

第四步：模型蒸馏
├── 教师模型：云端大模型
├── 学生模型：Pico Claw（7B-13B）
└── 部署到AIX Box本地运行
```

### 方案三：Search/Open/Find Skill Store

**将操作原语打包成Skill上架**：

| Skill | 功能 | 价格 |
|-------|------|------|
| **Search Skill** | 本地语料库检索 | 0 CH（离线） |
| **Open Skill** | 打开文档并解析 | 0 CH（离线） |
| **Find Skill** | 关键信息定位 | 0 CH（离线） |
| **深度研究组合包** | 三原语+Pico Claw推理 | 10 CH/次 |

---

## 与其他项目的协同

| 项目 | OpenResearcher 融合方式 |
|------|------------------------|
| **Firecrawl** | Firecrawl抓取网页 → OpenResearcher离线索引 → 本地检索 |
| **预测市场** | OpenResearcher深度研究 → 预测市场数据源 |
| **农产品溯源** | OpenResearcher检索溯源数据 → 本地验证 |
| **百万美元架构师** | Search/Open/Find用于知识库Skill |
| **SpecEyes** | 快系统：本地检索，慢系统：OpenResearcher深度推理 |

---

## 孵化项目方向

### 项目21：AIX Box 离线搜索引擎

**问题**：那耶村、创作者、预测市场都需要深度研究，但云端API成本高

**解决方案**：
- 移植OpenResearcher检索引擎
- 本地语料库（IPFS存储）
- Search/Open/Find Skill
- Pico Claw本地推理

**Coin Hour 节省**：80%+

**适合背景**：搜索引擎、信息检索、NLP

---

## 金句

> "OpenResearcher 让深度研究不再依赖云端API，AIX Box 让深度研究跑在村里。"

> "Search/Open/Find 是人类学者的浏览方式，也是 AIX Box 的研究原语。"

> "9.7万条轨迹训练的模型超越GPT-4，证明开源深度研究可行——那耶村也能有自己的深度研究智能体。"

---

## 总结

**OpenResearcher 与 AIX 的契合度**：★★★★☆

| 维度 | 契合点 |
|------|--------|
| 离线检索 | AIX Box 本地搜索引擎 |
| 操作原语 | Search/Open/Find Skill化 |
| 训练方法 | 那耶村深度研究轨迹合成 |
| 模型蒸馏 | 30B → 7B-13B 部署到Box |
| 成本优化 | 80%+ Coin Hour节省 |

**一句话**：OpenResearcher 让深度研究智能体可以从云端搬到 AIX Box 本地，让每个社区都有自己的"研究学者"。

---

*生成时间：2026-04-20（UTC）*
