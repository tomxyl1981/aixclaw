# 靶点引擎置信度公式白皮书 (confidence_formula.md)

> 版本: v1.0 | 2026-07-31 | AIXBox 靶点发现引擎
> 目标读者: Biotech / 药企研发 / CRO 合作方
> 目的: 让任何一位科学家都能读懂"置信度数字是怎么算出来的"

---

## 0. 一句话摘要

> **置信度不是 AI 拍脑袋的数字。它是三条独立证据链的数学合成：**
> ① 每条证据自身的质量（样本量 × 显著性 × 效应量 × 时效性）
> ② 证据矩阵的结构（维度覆盖 × 信号强度 × 一致性调整）
> ③ 不确定性审计（熵态校验：多源碰撞是否收敛）

任何数字都可以逐层拆开，追溯到原始论文。

---

## 1. 三层架构总览

```
┌─────────────────────────────────────────────────┐
│  Layer 3: 熵态校验 (不确定性审计)                  │
│  natural_entropy / pressure_ratio / 共识类型      │
│  → 回答: 这个数字"有多确定"                       │
├─────────────────────────────────────────────────┤
│  Layer 2: 矩阵级综合 (Kakeya 双因子)              │
│  weighted_score = 0.80×覆盖分 + 0.20×强度分       │
│    + 一致性boost - 矛盾扣分 - 缺失扣分             │
│  → 回答: 证据体系"有多完整"                       │
├─────────────────────────────────────────────────┤
│  Layer 1: 行级权重 (data_weight)                  │
│  w = 样本量分 × 显著分 × 效应分 × 时效分           │
│  → 回答: 单条证据"有多可靠"                       │
└─────────────────────────────────────────────────┘
         ↑ 每层都可追溯，每层都有明确公式
```

---

## 2. Layer 1: 行级权重（单条证据质量）

**公式**（`row_weight.py` → `compute_data_weight`）：

```
data_weight = logN_weight × pval_weight × effect_weight' × recency_weight
其中 effect_weight' = 0.5 + 0.5 × effect_weight
```

| 分量 | 公式 | 说明 |
|------|------|------|
| **样本量分** logN_weight | `min(log10(N+1) / 7, 1.0)` | N=样本量；1万样本≈0.57，1000万样本封顶1.0 |
| **显著分** pval_weight | `min(-log10(p) / -log10(5e-8), 1.0)`，p>0.05 时取 0.15 | p=5e-8 得满分；p=0.05 得 0.15 |
| **效应分** effect_weight | `min(|效应量| / 1.0, 1.0)` | OR/log2FC/β 绝对值；效应量≥1.0 封顶 |
| **时效分** recency_weight | `0.5 + 0.5 × max(0, 1 - 年龄/7年)` | 7年内线性衰减，7年前证据只剩 0.5 |
| **安全折扣** | 安全维度 × 0.7 | 安全性数据天然保守加权 |
| **兜底** | 无数值字段时：强度枚举 × 0.35 | 防止缺数值字段时权重为0 |

**示例**：一条 GWAS 证据，N=458,000，p=3e-9，OR=1.35，2023年发表
```
logN_weight = log10(458001)/7 = 5.66/7 = 0.808
pval_weight = 8.52/7.30 = 1.0 (封顶)
effect_weight' = 0.5 + 0.5×1.0 = 1.0 (OR 1.35 ≥ 1.0 封顶)
recency_weight = 0.5 + 0.5×(1-3/7) = 0.786
data_weight = 0.808 × 1.0 × 1.0 × 0.786 = 0.635
```

---

## 3. Layer 2: 矩阵级综合（Kakeya 双因子）

**公式**（`target_evidence_matrix.py` → `compute_weighted_confidence`）：

```
dual_score = 0.80 × coverage_score + 0.20 × intensity_score

final_score = clamp(
    dual_score + consistency_boost − contradiction_penalty − missing_penalty,
    0.0, 1.0
)
```

### 3.1 维度覆盖分（占 80%）

```
coverage_score = min(已覆盖维度数 / 4.0, 1.0)
```
- 6 个核心维度：GWAS / scRNA-seq / 动物模型 / 可药性 / 通路 / 安全性
- **≥4 个维度封顶**——证据冗余性：4 个独立维度已足够支撑判断，更多不额外加分
- 这就是"多重独立验证"的数学化：4 个独立实验方向一致，比 1 个实验做 10 次更可信

### 3.2 证据强度分（占 20%）

```
intensity_score = min(Σ strength_weight / Σ 全部强度权重, 1.0)
```

| 强度枚举 | 权重 | 对应生物信号 |
|---------|:----:|------------|
| P_VALUE_LT_5E8 | 0.08 | 全基因组显著 |
| AUC_GT_0_8 | 0.07 | 强分类能力 |
| LOG2FC_GT_1 | 0.07 | 强差异表达 |
| P_VALUE_LT_1E5 | 0.06 | 强候选显著 |
| AUC_GT_0_6 | 0.05 | 中等分类能力 |
| LOG2FC_GT_0_5 | 0.05 | 中等差异表达 |
| P_VALUE_LT_0_01 | 0.04 | 名义显著 |
| NOMINAL | 0.03 | 弱显著 |
| WEAK / UNKNOWN | 0.02 | 弱信号 |
| NOT_SIGNIFICANT | 0.01 | 不显著 |

### 3.3 调整项

| 调整 | 规则 | 幅度 |
|------|------|:----:|
| **一致性 boost** | 同一维度同一方向 ≥2 条证据 | 每组 +0.05 |
| **矛盾扣分** | 每条矛盾记录（含跨维度方向冲突） | 每条 −0.10 |
| **缺失扣分** | 每个高优先级缺失维度 | 每个 −0.10 |

> 设计哲学：**方向一致的重复加分很少，方向冲突的扣分很重**——
> 因为矛盾是信息，重复不是。这正是邓煜熵态框架的落地。

---

## 4. Layer 3: 熵态校验（不确定性审计）

**公式**（`entropy_section.py` → `compute_entropy`）：

```
natural_entropy = H(各维度自然置信度分布)     # 0=完全确定, 1=完全不确定
pressure_ratio = (processed_confidence − natural_confidence) / natural_confidence
consensus_type ∈ {full_consensus, majority, split, no_consensus}
```

| 指标 | 含义 | Biotech 对应物 |
|------|------|---------------|
| **自然态熵 H₀** | 各独立证据源在"未处理"状态下碰撞产生的分歧度 | 多中心试验结果的一致性 |
| **压熵幅度** | 系统处理后置信度被压缩/抬升的比例 | 数据清洗对结论的影响 |
| **共识类型** | 独立维度方向收敛程度 | 重复实验的收敛度 |

**决策映射**（`render_entropy_section`）：

```
full_consensus + H₀ < 0.3  → 🟢 可信区间（可直接决策）
full_consensus/majority + H₀ < 0.6 → 🟡 有条件可信（补充证据后决策）
其他（熵高/矛盾多/共识弱） → 🔴 高不确定性（优先投入验证）
```

---

## 5. 端到端示例：ACVR2A → 肝纤维化

**输入**：8 条证据行，6 个维度（GWAS×2、scRNA×2、动物模型、可药性、通路、安全）

| 步骤 | 计算 | 结果 |
|------|------|:----:|
| Layer 1 | 8 条行级 data_weight 均值 | 0.42–0.71 |
| Layer 2 | coverage = min(6/4,1)=1.0；intensity=0.86 | dual=0.97 |
| 调整 | +0.10 boost −0.10 矛盾 −0.10 缺失 | final=0.87 |
| Layer 3 | H₀=0.527，pressure=0.62 | 🟡 有条件可信 |

> 完整输出见简报第九章「熵态分析」。

---

## 6. 边界与限制（诚实声明）

1. **当前置信度 = 证据组织的置信度，不是因果的置信度**。它衡量"证据体系有多完整一致"，不承诺"这个靶点一定有效"。
2. **Phase 3 升级路径**：MR 因果效应 + Bayesian 后验更新上线后，公式将升级为
   `P(有效|证据) = 先验 × Π 各证据源似然`，届时置信度才具备概率解释。
3. **权重参数目前为专家启发式设定**（Kakeya 双因子 + 强度表），尚未用历史靶点数据校准——校准计划在 Phase B 验证环节执行。
4. **每个数字都可逆推**：任何输出都附带 weights_raw / adjustments / 维度明细，可逐层复核。

---

## 7. 快速问答（给 Biotech 同事）

| 问题 | 回答 |
|------|------|
| 0.87 是什么意思？ | 证据体系覆盖 6 个独立维度、方向基本一致、矛盾极少。不是"87% 有效概率" |
| 为什么重复实验只加 0.05？ | 重复只证明"可复现"，不证明"正确"。独立维度才提供新信息 |
| 为什么矛盾扣 0.10？ | 矛盾意味着某个证据源可能系统偏差，是重要信号，必须重罚 |
| 缺失维度也扣分？ | 是的。不知道的维度（如动物模型缺失）是真实不确定性，不能装作知道 |
| 怎么验证你们的数字？ | 简报附全部 PMID + 数据源链接 + 每层中间值，点开即可复核 |

---

> 维护: AIXBox 靶点发现引擎 | 配套代码: `row_weight.py` / `target_evidence_matrix.py` / `entropy_section.py`
> 下次评审: Phase B (MR 管线) 交付后，本公式将并入 Bayesian 后验框架
